{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "dev:api": "bash -c 'set -a && source .env && set +a && export PORT=$API_PORT && pnpm --filter @workspace/api-server dev'",
    "dev:web": "bash -c 'set -a && source .env && set +a && export PORT=$WEB_PORT && pnpm --filter @workspace/whatsapp-hub dev'",
    "db:push": "bash -c 'set -a && source .env && set +a && pnpm --filter @workspace/db push'"
  },
  "private": true,
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
